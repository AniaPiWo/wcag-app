
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

model AuditRequest {
  id              String   @id @default(uuid())
  url             String
  name            String   @default("")
  email           String   @default("")
  status          String   @default("pending") // pending, in-progress, completed, failed
  completedAt     DateTime?
  auditType       String   @default("automated") // automated, manual
  
  // podsumowanie audytu automatycznego
  totalIssuesCount Int?     @default(0)
  criticalCount   Int?     @default(0)
  seriousCount    Int?     @default(0)
  moderateCount   Int?     @default(0)
  minorCount      Int?     @default(0)
  passedRules     Int?     @default(0)
  incompleteRules Int?     @default(0)
  timestamp       String?
  
  // podsumowanie audytu
  violations      String?  @db.String // JSON z naruszeniami
  aiAnalysis      String?  @db.String // Analiza AI wyników audytu
  
  // dane dla manualnego audytu
  basicAudit      String?  @db.String // JSON z odpowiedziami dla podstawowego poziomu
  intermediateAudit String? @db.String // JSON z odpowiedziami dla średniego poziomu
  advancedAudit   String?  @db.String // JSON z odpowiedziami dla zaawansowanego poziomu
  selectedLevels  String?  @db.String // JSON z wybranymi poziomami audytu
  
  // pola systemowe
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  errorMessage    String?  // W przypadku niepowodzenia
}
